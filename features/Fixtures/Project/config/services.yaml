parameters:
    locale: 'en'

services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: false

    KunicMarko\SonataAutoConfigureBundle\Features\Fixtures\Project\:
        resource: '../src/*'
        exclude: '../src/{Entity,Kernel.php}'
